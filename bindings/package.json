{
  "name": "route_annotator",
  "version": "1.0.0",
  "description": "Bindings for route-annotator",
  "keywords": [
    "addon",
    "native",
    "module"
  ],
  "author": "Mapbox",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "install": "ncmake --build --config Release",
    "test": "node -p \"const pkg = require('./index'); const annotator = new pkg.Annotator(42); console.log(annotator); console.log(annotator.getValue())\""
  },
  "devDependencies": {
    "nan": "^2.2.1",
    "node-cmake": "^1.2.0"
  }
}
