#include "hashmap.hpp"

    void Hashmap::add(external_nodeid_t to, external_nodeid_t from, speed_t speed) {};
    bool Hashmap::hasKey(external_nodeid_t to, external_nodeid_t from) { return true;};

    void Hashmap::loadData(std::ifstream input) {};
    speed_t Hashmap::getValue(external_nodeid_t to, external_nodeid_t from){ return 0;};